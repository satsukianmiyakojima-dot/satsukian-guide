# さつきあん ゲストガイド

沖縄・宮古島の民宿「さつきあん宮古島」の宿泊客専用デジタルガイド。単一の静的HTMLファイル(`index.html`)で、ビルド不要・依存パッケージなし。

## 構成

- `index.html` — アプリ本体(HTML/CSS/JSすべて1ファイルに内包)
- `apple-touch-icon.png` — ホーム画面用アイコン(180×180、青緑地に白文字「さつきあん」+ミンサー柄の帯)。`index.html` の `<link rel="apple-touch-icon">` と `<meta name="apple-mobile-web-app-title">` が参照。ご案内タブの先頭に「ホーム画面に追加する方法」(iPhone/Android、4言語、`a2hsHtml()`)がある。
- `content/satsukian_guide_content.xlsx` — 表示内容(4言語)のマスターデータ。内容を変更する際はこのExcelを直接編集してから `index.html` に反映する運用

## 内容の言語

日本語・英語・繁体中文(台湾からの旅行者向け)・韓国語をボタンで切り替え。`index.html` 内の `const T = {...}` オブジェクトに4言語分の文言がまとまっている。

## 重要な設計上の制約

- 宿泊客は運営者のClaudeアカウントの組織メンバーではない外部の一般利用者のため、Claude Artifactの `db` / `sample` などのランタイム機能は使用不可(組織内限定のため公開共有と両立しない)。そのため:
  - 表示内容はすべて `index.html` に静的に埋め込み(4言語分を事前翻訳済み)
  - オーナーへの連絡はメール(`mailto:`)リンクのみ
  - Wi-Fiパスワード等は実データをそのまま埋め込み(このファイルの取り扱いに注意)
- インタラクションは**すべて `addEventListener` によるイベント委譲**で実装している。`onclick="..."` のようなインライン属性ハンドラは、Claude Artifactの CSP でブロックされ動作しない実績があるため、今後も避けること。

## 実データ

- 住所: 沖縄県宮古島市下地川満879
- 電話(代表): 070-8350-6509 / 緊急連絡先: 090-5655-0299(倉橋)
- メール: satsukian.miyakojima@gmail.com
- お部屋: 102号室(最大8名・バリアフリー)/ 201号室(最大12名・屋上テラス)/ 202号室(最大10名・ファミリー向け)
- Wi-Fi SSID/パスワードは `content/satsukian_guide_content.xlsx` の「Wi-Fi_接続情報」シートが正。

## 「おすすめ」タブ(宿泊者限定のお店・割引)

- お店のデータは `index.html` 内の `const SHOPS = [...]` に1店ずつ入れる。`cat` は `food` / `car` / `activity`。文言は `{ja, en, zh, ko}` の4言語で持ち、`line` / `web` / `map` / `closed` / `distance` は空ならボタン・行を出さない。
- 写真は `SHOP_PHOTOS`(`{お店のid: 'data:image/jpeg;base64,...'}`)に埋め込む。幅約500px・JPEG品質80前後に縮小してから入れる(ファイルを増やさず、GitHubへのアップロードを `index.html` 1つで済ませるため)。
- 宿泊者の確認方法は「画面を見せる」方式。「この画面をお店の人に見せる」で全画面表示(今日の日付+動く時計)を出す。URLを知っていれば誰でも見られるので、完全な鍵ではない。
- お店の元データは、オーナーが `content/おすすめのお店_記入用.xlsx`(日本語のみ)に書く。Claudeが4言語に翻訳して `SHOPS` に反映する。
- 見本として「こめっとレンタカー」(レンタカー・宿泊者限定プラン)だけが入っている。

## 宿泊期間の入力(Wi-Fi・おすすめの表示制限)

- 画面の流れ: 言語選択 → **宿泊期間の入力** → チェックイン時の確認 → ガイド。QRは全員同じURL(お客様が自分で期間を入力する方式。スタッフ作業なし)。
- 入力した期間(`{in, out}`、JSTのYYYY-MM-DD)は `localStorage` の `sk_stay` に保存。**Wi-Fiタブとおすすめタブだけ**が、期間中(`active`)のみ表示される。期間前(`notyet`)・終了後(`expired`)は `lockedPanel()` の「見られません」画面。他のタブ(ご案内・ルール・BBQ・FAQ・お問い合わせ)は常に見られる。
- ルール: 最大14泊 / チェックイン日は「13日前〜明日」 / 宿泊中に直せるのはチェックアウト日だけ(チェックイン日は固定。日付を延ばし続けて使うのを防ぐ) / 終了後の期間は変更不可、新しい期間を入れられるのは前回のチェックアウトから30日後(`STAY_COOLDOWN_DAYS`)。新しい期間を入れると、確認画面と部屋選択がリセットされる。
- 日付は端末の設定ではなく `Asia/Tokyo` で判定・表示する。アプリを再び開いた時(`visibilitychange`)にも再判定し、期間が終わっていれば「お店に見せる画面」も閉じる。
- **限界**: お客様の自己申告なので、宿泊していない人が適当な日付を入れれば見られる。ページのソースにはWi-Fiパスワードが含まれる(画面上の非表示だけ)。これは「うっかり使い続ける」を防ぐための仕組みで、悪意のある利用は防げない。より確実にするなら、スタッフがお客様ごとに日付入りQRを作る方式や、予約表との照合(Apps Script等)を検討する(相談済み。オーナーは自己入力方式を選択)。

## 公開について

GitHub Pages(`satsukianmiyakojima-dot/satsukian-guide` の `main`)で公開している: https://satsukianmiyakojima-dot.github.io/satsukian-guide/

このパソコンのGit認証は別アカウントのため `git push` は403になる。更新は、オーナーがさつきあんのGitHubアカウントでWebのアップロード画面(`/upload/main`)にファイルをドラッグして反映する運用。
